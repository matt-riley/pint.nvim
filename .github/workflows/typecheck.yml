name: Typecheck

on:
  push:
  pull_request:

jobs:
  typecheck:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Install LuaLS
        run: |
          mkdir -p "${RUNNER_TEMP}/luals"
          curl -fsSL \
            "https://github.com/LuaLS/lua-language-server/releases/download/3.13.6/lua-language-server-3.13.6-linux-x64.tar.gz" \
            | tar -xz -C "${RUNNER_TEMP}/luals"

      - name: Run typecheck
        run: |
          mkdir -p "${RUNNER_TEMP}/luals-log"
          "${RUNNER_TEMP}/luals/bin/lua-language-server" \
            --check=. \
            --checklevel=Warning \
            --logpath="${RUNNER_TEMP}/luals-log" \
            || true

      - name: Upload diagnostics
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: luals-diagnostics
          path: ${{ runner.temp }}/luals-log/check.json
          if-no-files-found: error

      - name: Report diagnostics
        run: |
          python - <<'PY'
          import json
          import os
          import pathlib
          import sys

          path = pathlib.Path(os.environ["RUNNER_TEMP"]) / "luals-log" / "check.json"
          if not path.exists():
              print("No LuaLS diagnostics file generated.")
              sys.exit(1)

          diagnostics = json.loads(path.read_text() or "[]")
          if not diagnostics:
              print("LuaLS diagnostics: none")
              sys.exit(0)

          print(json.dumps(diagnostics, indent=2))
          sys.exit(1)
          PY
