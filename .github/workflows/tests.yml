name: Tests

on:
  push:
  pull_request:

jobs:
  tests:
    uses: matt-riley/matt-riley-ci/.github/workflows/ci.yml@071c2c2
    with:
      install: true
      test: true
      task-env: |
        MINI_PATH=${{ github.workspace }}/.ci/mini.nvim